# Copyright (c) 2022 Graphcore Ltd. All rights reserved.
import lit.formats
import os

# lit sensibly does not inherit the parent process's environment, but
# unfortunately our tests require a lot of environment variables so copy
# them here.
config.environment = os.environ

config.name = 'poptorch lit tests'
config.test_format = lit.formats.ShTest()

config.suffixes = ['.mlir']

config.substitutions.append(('poptorch-opt', lit_config.params["poptorch-opt"]))
config.substitutions.append(('FileCheck', lit_config.params["FileCheck"]))
