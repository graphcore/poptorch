#!/bin/bash
POPTORCH_ROOT=$(cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd)
export @LD_PATH_VARNAME@=${POPTORCH_ROOT}/@INSTALL_PYDIR@${@LD_PATH_VARNAME@:+:${@LD_PATH_VARNAME@}}
export PYTHONPATH=${POPTORCH_ROOT}:$PYTHONPATH
