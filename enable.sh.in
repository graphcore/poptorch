#!/bin/bash
POPTORCH_ROOT=$(cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd)
export LIBRARY_PATH=${POPTORCH_ROOT}/@CMAKE_INSTALL_LIBDIR@${LIBRARY_PATH:+:${LIBRARY_PATH}}
export @LD_PATH_VARNAME@=${POPTORCH_ROOT}/@CMAKE_INSTALL_LIBDIR@${@LD_PATH_VARNAME@:+:${@LD_PATH_VARNAME@}}
export PYTHONPATH=${POPTORCH_ROOT}/python:$PYTHONPATH
