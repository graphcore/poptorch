# IMPORTANT: Keep requirements in sync with ./config.buildenv.py

--find-links https://download.pytorch.org/whl/torch_stable.html
pytest==6.2.1
transformers==4.12.2
sphinx==3.3.1
sphinx_rtd_theme==0.5.0
breathe==4.25.1
docutils==0.16
scipy==1.5.4
torch==1.10.0+cpu; sys_platform != "darwin" and platform_machine != "aarch64"
torchvision==0.11.1+cpu; sys_platform != "darwin" and platform_machine != "aarch64"
torch==1.10.0; sys_platform == "darwin" or platform_machine == "aarch64"
torchvision==0.11.1; sys_platform == "darwin" or platform_machine == "aarch64"
tqdm==4.46.1
expecttest==0.1.3
# Use old version for wheel.pep425tags support (new versions removed it).
wheel<0.35
