# IMPORTANT: Keep requirements in sync with ./config.buildenv.py

--find-links https://download.pytorch.org/whl/torch_stable.html

torch==2.0.0+cpu; sys_platform != "darwin" and platform_machine != "aarch64"
torch==2.0.0; sys_platform == "darwin" or platform_machine == "aarch64"
torchaudio==2.0.1+cpu; sys_platform != "darwin" and platform_machine != "aarch64"
torchaudio==2.0.1; sys_platform == "darwin" or platform_machine == "aarch64"
torchvision==0.15.1+cpu; sys_platform != "darwin" and platform_machine != "aarch64"
torchvision==0.15.1; sys_platform == "darwin" or platform_machine == "aarch64"

expecttest==0.1.3
lit==0.11.1
pytest==6.2.5
setuptools==58.0.4
tqdm==4.46.1
transformers==4.12.2
typing-extensions==4.1.1
# Use old version for wheel.pep425tags support (new versions removed it).
wheel<0.35

-r poptorch_geometric/requirements.txt